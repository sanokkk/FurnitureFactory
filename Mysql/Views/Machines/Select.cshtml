@model List<machine>
@{
    ViewData["Title"] = "Select";

    Pager pager = new Pager();

    int pageNo = 0;
    var purpose = ViewBag.Purpose;

    if (ViewBag.Pager != null)
    {
        pager = ViewBag.Pager;
        pageNo = pager.CurrentPage;
    }
}


@{
    foreach(var el in Model)
    {
    <div class="card" style="padding:10px;">
        <h5 class="card-title">Machine @el.id_machine</h5>
        <p class="card-text">Назначение: @purpose[el.id_purpose - 1].purpose</p>
        <div>
                <button>
                    <a asp-action="Delete" asp-controller="Machines" asp-route-id="@el.id_machine" class="btn btn-link" style="width:100% ">Удалить</a>
                </button>
                <button>
                    <a asp-action="Update" asp-controller="Machines" asp-route-id="@el.id_machine" class="btn btn-link" style="width:100%">Изменить</a>
                </button>
                </div>
    </div>
    }
}
<div class="card" >
            <button style="width=100%">
                <a asp-action="Add" asp-controller="Machines"  class="btn btn-link" style="width:100%">Добавить</a>
            </button>
            
        </div>

            
         



        <div class="container">
            @if (pager.TotalPages > 0)
    {
                <ul class="pagination justify-content-end">

                    @for (var pge = pager.StartPage ;pge <= pager.EndPage; pge++)
            {
                    <li class="page-item @(pge == pager.CurrentPage ? "active" : "")">
                        <a class="page-link" asp-controller="Machines" asp-action="Select" asp-route-pg="@pge"> @pge </a>
                    </li>
            }

                </ul>
    }
    </div>

