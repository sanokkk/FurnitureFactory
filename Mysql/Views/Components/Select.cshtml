@model List<component>
@{
    ViewData["Title"] = "Select";

    Pager pager = new Pager();

    int pageNo = 0;

    var mt = ViewBag.Material;

    if (ViewBag.Pager != null)
    {
        pager = ViewBag.Pager;
        pageNo = pager.CurrentPage;
    }
}


@{
    foreach(var el in Model)
    {
    <div class="card" style="padding:10px;">
        <h5 class="card-title">Component @el.id_component</h5>
        <p class="card-text">@el.name_component</p>
        <p class="card-text">Материал: @mt[el.id_material - 1].name_material</p>
        <div>
                <button>
                    <a asp-action="Delete" asp-controller="Components" asp-route-id="@el.id_component" class="btn btn-link" style="width:100% ">Удалить</a>
                </button>
                <button>
                    <a asp-action="Update" asp-controller="Components" asp-route-id="@el.id_component" class="btn btn-link" style="width:100%">Изменить</a>
                </button>
                </div>
    </div>
    }
}
<div class="card" >
            <button style="width=100%">
                <a asp-action="Add" asp-controller="Components"  class="btn btn-link" style="width:100%">Добавить</a>
            </button>
            
        </div>

            
         



        <div class="container">
            @if (pager.TotalPages > 0)
    {
                <ul class="pagination justify-content-end">

                    @for (var pge = pager.StartPage ;pge <= pager.EndPage; pge++)
            {
                    <li class="page-item @(pge == pager.CurrentPage ? "active" : "")">
                        <a class="page-link" asp-controller="Components" asp-action="Select" asp-route-pg="@pge"> @pge </a>
                    </li>
            }

                </ul>
    }
    </div>

